# Core dependencies
python-dotenv==1.0.0

# AI/ML APIs - UPDATED VERSIONS
cerebras-cloud-sdk>=1.0.0,<2.0.0
cohere>=5.0.0,<6.0.0

# Speech Recognition & Translation
selenium==4.15.0
webdriver-manager==4.0.1
mtranslate==1.8

# Text-to-Speech
pyttsx3==2.90

# Automation
pyautogui==0.9.54
requests==2.31.0

# System Control (Windows only)
pycaw==20230407;platform_system=="Windows"
comtypes==1.2.0;platform_system=="Windows"

# Rich terminal output
rich==13.7.0

# Image Generation
Pillow==10.1.0

# GUI (usually comes with Python)
# If tkinter missing, install python3-tk on Linux

# Notes:
# - For Windows: All packages should work out of the box
# - For Linux/Mac: You may need additional system dependencies
# - pycaw and comtypes are Windows-only (for volume control)
# - Install Chrome browser before running SpeechToText.py
# - ChromeDriver will be auto-downloaded by webdriver-manager

# Installation command:
# pip install -r Requirements.txt

# If you get version conflicts, try:
# pip install --upgrade cerebras-cloud-sdk
# pip install --upgrade cohere